[package]
name = "bario-pdk"
version = "0.1.0"
edition = "2021"
description = "Write bario modules in Rust"
license = "MIT"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
